{
  "name": "ws2023-tcpudp-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "18.x.x"
  },
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "npx tsc",
    "gcp-build": "npm run build",
    "deploy": "gcloud app deploy",
    "production-start": "npm install && npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.17",
    "@types/express-validator": "^3.0.0",
    "@types/node": "^20.2.3",
    "@types/node-cron": "^3.0.7",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "node-cron": "^3.0.2",
    "pg": "^8.11.0",
    "postgres": "^3.3.4",
    "typeorm": "^0.3.16",
    "typescript": "^5.0.4"
  }
}
